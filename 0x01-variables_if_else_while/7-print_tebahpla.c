#include <stdio.h>

/**
 * main - prints single digit numbers 
 *
 * Return: Always 0.
 */

int main(void)
{
	char a;

	a = 'z';
	while
		(a >= 'a')
		{
			putchar(a);
			a--;
		}
	putchar('\n');
	
	return (0);
}
