#include <stdio.h>

/**
 * _strlen - returns the length of a string
 * @str: The string to print
 * Return: void
 */

int _strlen(char *str)
{
	int length = 0;

	while (str++)
	length++;
	return (length);
}
