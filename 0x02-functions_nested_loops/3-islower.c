#include "main.h"

/**
 * _islower - check for lowercase character
 *
 * Return: 1 for lowercase character or 0 for anything else
 */

int _islower(void)

{

	if (c >= 97 && c <= 122)
	{

		return (1);
	}
	return (0);
}
